language: java
jdk:
  - openjdk8
cache:
  directories:
    - $HOME/.m2
script: "mvn cobertura:cobertura"
after_success:
  - bash <(curl -s https://codecov.io/bash)
branches:
  only:
    - master
    - "/^\\d{1}\\.\\d{1,2}.\\d{1,2}$/"
notifications:
  email:
    recipients:
      - hi@xuyuanxiang.cn
    on_success: never
    on_failure: always
